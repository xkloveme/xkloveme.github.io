name: docs

on:
  push:
    branches: [master]
  workflow_dispatch:

jobs:
  docs:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4  # 升级到 v4
        with:
          fetch-depth: 0

      - name: Install Bun
        run: |
          curl -fsSL https://bun.sh/install | bash
          echo "${HOME}/.bun/bin" >> $GITHUB_PATH

      - name: Cache dependencies
        uses: actions/cache@v4
        with:
          path: |
            node_modules
            ～/.bun  # 修正路径，确保缓存生效
          key: ${{ runner.os }}-bun-${{ hashFiles('**/bun.lockb') }}
          restore-keys: |
            ${{ runner.os }}-bun-

      - name: Install dependencies
        run: bun install

      - name: Build site
        run: bun run build

      - name: Deploy to GitHub Pages
        uses: JamesIves/github-pages-deploy-action@v4.5.2  # 建议升级到最新版
        with:
          # 🔴 删除 ACCESS_TOKEN，直接使用默认的 GITHUB_TOKEN
          BRANCH: gh-pages
          FOLDER: public
          # 如果你的 GitHub Pages 设置里源分支是 gh-pages，这行可以省略
          # CLEAN: true # 可选：部署前清理旧文件